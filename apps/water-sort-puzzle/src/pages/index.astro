---
import "~/styles/game.css";
import Game from "~/components/Game.tsx";
import Layout from "~/layouts/Layout.astro";

const id = "water-sort-puzzle";
const title = "Water Sort Puzzle";
const canonical = `https://games.rman.dev/${id}`;
const description = `Classic Water Sort Puzzle game by Roman. Play on ${canonical}`;
const thumbnail = `/thumbnails/${id}.png`;
---

<Layout {title} {description} {canonical} {thumbnail}>
  <Game client:load />
</Layout>
